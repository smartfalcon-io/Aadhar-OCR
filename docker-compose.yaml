version: "3.9"

services:
  fastapi-ocr:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastapi-ocr-container
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
    restart: always
